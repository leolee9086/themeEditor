
            // 使用线性插值函数计算当前的速度
           /* const speed = lerp(initialSpeed, finalSpeed, progress);
            function lerp(start, end, progress) {
              return start + (end - start) * progress;
            }*/